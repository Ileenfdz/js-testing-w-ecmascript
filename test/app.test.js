import { xName } from '../src/app.js';

describe('descripción general del fin de los tests que se vayan a hacer', () => {
    test('Descripción del test en concreto', () =>{
            expect(xName(1)).toBe(2);
    })
});